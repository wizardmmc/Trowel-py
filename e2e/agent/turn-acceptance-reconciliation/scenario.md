# AGT-004：发送结果未知时只对账，不重发

## 用户场景

用户发送一条 Codex 消息后，桌面没有收到“本轮已接受”的 HTTP 回包，但后端已经开始原
来的轮次并显示命令审批。Trowel 应把这次发送标为结果待确认，继续用活动快照和实时事件
找回原 turn；等待期间输入框保持禁用，不能自动再发一遍，也不能并发开始第二轮。

用户处理原审批后，本轮正常结束。服务端历史中只能有一条用户消息和一个对应终态；出现
第二次 turn POST、丢掉已接受的 turn ID、允许并发输入或产生两份副作用都算失败。
