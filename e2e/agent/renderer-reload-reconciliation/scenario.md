# AGT-006 / SSE-001 / SSE-002：审批中断线后恢复同一轮

## 用户场景

Codex 已经接受消息并停在命令审批时，renderer 被重载，应用级 SSE 随之断开。页面重新
连接后应从活动快照、历史和待处理请求中找回同一条会话、同一个 turn 和原审批，输入框
继续禁用；不能把 prompt 再发一次，也不能把审批挂到一条新轮次。

用户拒绝审批后再次重载页面，历史里仍只有一条用户消息、一个审批结果和一个终态，界面
不重复显示。会话关闭后 owner 归零；请求丢失、重复消息、旧终态覆盖新轮或残留资源都算
失败。
