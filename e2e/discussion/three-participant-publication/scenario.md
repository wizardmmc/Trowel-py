# DIS-001：三位参与者共同公开

## 用户场景

用户进入研讨页，写下一个问题，选中工作区，再创建三位有明确运行配置的参与者。点击
“创建并开始第 1 轮”后，三位参与者收到完全相同的冻结问题，各自在自己的原生会话里
作答。某一位先答完时可以看到它已经结束，但正文不能提前混进公开记录。

只有三个位都进入终态，第一轮才一次性显示为 `3/3 同时公开`。页面、Discussion API、
publication 和三个结果槽必须一致，不能发布两次或少一位。查看参与者配置和历史记录后，
停止研讨应关闭三条 participant session；任一会话仍留在资源账本里都算失败。
