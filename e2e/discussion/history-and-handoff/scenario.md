# DIS-005：历史、详情与普通 Agent 交接

## 用户场景

一轮三人研讨已经共同公开。页面左侧历史里能找到这场研讨，右侧参与者状态能看到三位
被冻结的运行配置和当前状态；重新点开历史时，公开结果、参与者顺序和轮次不会丢失。

用户点击“在 Agent 中继续”，写下下一步指令并选择普通 Claude 连接。Trowel 创建的是一
条正常用户 Agent 会话，把已公开研讨现场作为背景，再把指令作为首轮输入发送。交接会话
不能冒充 discussion participant，也不能把未公开内容带出去。关闭普通 Agent，再回到历史
停止研讨时，两边资源都应分别归零。
