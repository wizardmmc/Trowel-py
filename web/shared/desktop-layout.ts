/** 集中定义 Electron Host 与 renderer 共用的桌面布局像素尺寸。 */

export const DESKTOP_LAYOUT_PX = {
  sidebarWidth: 84,
  multiSessionWidth: 216,
  leftColumnsWidth: 300,
  mainShoulderRadius: 18,
  topDragHeight: 48,
  trafficLightX: 14,
  trafficLightY: 17,
} as const;
